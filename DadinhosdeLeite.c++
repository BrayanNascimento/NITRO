#include <iostream>
using namespace std;

int main()
{
  // Escreva seu código aqui
  int pessoas, dadinhos, centavos;
  cin >> pessoas >> dadinhos >> centavos;
  int cal = 0;
  while (pessoas, dadinhos, centavos != 0)
  {
    cal = (pessoas * dadinhos) * centavos;
    cout << cal << endl;
    cin >> pessoas >> dadinhos >> centavos;
  }
  return 0;
}