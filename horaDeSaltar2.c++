#include <iostream>
#include <math.h>
#include <iomanip>
using namespace std;

int main()
{
  // Escreva seu código aqui

  // 1 pegar a distancia a ser percorrida
  // 2 pegar a distancia de cada salto
  // 3 fazer o calculo

  int n = 0, x, y, i = 0;

  cin >> x >> y;

  for (i; n < x; i++)
  {
    n = n + y;
    // cout << y << endl;
  }
  cout << i;
  return 0;
}